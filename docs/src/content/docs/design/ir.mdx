---
title: IR
---

DPScript technically has an IR language - but it's not compilable. It's just a representation
of the internal AST that's easier to read than super-verbose dumps, made for debugging the compiler (mainly).
